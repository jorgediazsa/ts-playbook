{
  "name": "ts-senior-roadmap-basic-1-3",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.type-tests.json --noEmit",
    "lint": "eslint .",
    "verify": "pnpm lint && pnpm typecheck && pnpm test",
    "advanced:17:build": "tsc -b advanced/17-large-scale-architecture-patterns/fixtures/monorepo/tsconfig.json",
    "advanced:18:typecheck": "tsc -p advanced/18-migration-integration-strategies/fixtures/legacy-js/tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.56.0",
    "vitest": "^2.0.0"
  }
}